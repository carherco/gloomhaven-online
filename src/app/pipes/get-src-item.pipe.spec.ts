import { GetSrcItemPipe } from './get-src-item.pipe';

describe('GetSrcItemPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSrcItemPipe();
    expect(pipe).toBeTruthy();
  });
});
