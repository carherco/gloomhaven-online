import { FilterItemsByTipePipe } from './filter-items-by-tipe.pipe';

describe('FilterItemsByTipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterItemsByTipePipe();
    expect(pipe).toBeTruthy();
  });
});
